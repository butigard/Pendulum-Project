dPendulum p;
PGraphics canvas;
float cx;
float cy;
float t = 0;
float dt = .2;
int iter = 0;
float a1 = PI/2;
float a2 = PI/2;
float r1 = 200;
float r2 = 200;
float m1 = 40;
float m2 = 40;
float gravity = 9.81;

int fRate = 45;
int uRate = 45;

public void settings() {
  size(900,900);
}

void setup() {
  frameRate(fRate);
  cx = width/2;
  cy = height/10;
  canvas = createGraphics(width, height);
  canvas.beginDraw();
  canvas.background(255);
  canvas.endDraw();
  
  
  p = new dPendulum(new PVector(cx, cy), r1, r2, m1, m2, gravity, a1, a2);
}

void draw() {
  background(255);
  p.go();
}

void mousePressed() {
p.clicked(mouseX,mouseY);
}

void mouseReleased() {
p.stopDragging();
}
